# DocTranslate — Python Translation Microservice
#
# IMPORTANT — install CPU-only PyTorch first to avoid downloading 2 GB of
# CUDA packages (argostranslate depends on stanza which depends on torch):
#
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements_translator.txt
#
# run.bat does this automatically. Only follow the manual steps above if
# you are installing without run.bat.

# Translation engine
argostranslate>=1.9.0

# PDF rasterisation — converts PDF pages to images for OCR.
# OCR itself runs via Windows.Media.Ocr (built into Windows 10/11, CPU-native).
pymupdf>=1.23.0
